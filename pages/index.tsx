import React from "react";
import App  from "components/App";

const Home: React.FunctionComponent = () => {
    return (
        <React.Fragment>
            <App />
        </React.Fragment>
    );
};

export default Home;
